# 单实例保护变更记录

日期：2026-05-06  
项目：`D:\Projects\course-assistant\course-assistant-V2-integrated-20260505-235000`

## 本次修改目的

为课程助手增加单机器人实例保护，避免同一个 QQ 机器人被多个 Python 进程重复启动，导致：

- 同一条消息被多个实例重复处理
- WebSocket 会话互相顶掉、反复重连
- 机器人回复前后矛盾
- 排查问题时难以确认当前在线实例

## 修改内容

### 1. 新增运行时单实例守卫

新增文件：

- `src/runtime/__init__.py`
- `src/runtime/single_instance.py`

实现内容：

- 使用 Windows 可用的 `msvcrt.locking` 对锁文件加独占锁
- 第一个启动的实例成功持有锁
- 第二个实例启动时直接退出，不再继续连接 QQ 网关
- 服务退出时释放锁

### 2. 在主启动入口接入单实例保护

修改文件：

- `src/main.py`

接入方式：

- 在 `asyncio.run(main())` 之前先尝试获取单实例锁
- 若锁已被占用，则写入日志并以退出码 `1` 结束进程
- 启动成功后，在进程退出时释放锁

## 运行记录文件

单实例保护会在 `run_logs` 目录下生成以下文件：

- `course_assistant.lock`
  - 锁文件
- `course_assistant.instance.json`
  - 当前持有实例的记录
  - 包含 PID、启动时间、Python 路径、工作目录、QQ AppID、sandbox 状态
- `course_assistant.instance.log`
  - 事件历史记录
  - 记录 `started`、`lock_denied`、`stopped`

## 本次验证结果

### 编译检查

已通过：

- `src/main.py`
- `src/runtime/single_instance.py`

### 启动验证

验证时间：2026-05-06 19:56 左右

第一实例：

- PID：`41796`
- 状态：启动成功

日志确认：

- `Single-instance lock acquired`
- `Qclaw mode: real`
- `Qclaw bot starting: appid=1903962169 sandbox=False`
- `机器人「机器人1903962169」启动成功！`

### 重复启动拦截验证

第二实例测试：

- PID：`27628`
- 状态：被拦截后退出

对应日志：

- `Service start blocked by single-instance guard: Another service instance is already running...`

## 当前结论

单实例保护已生效。  
现在同一时刻只能保留一个课程助手服务实例在线。

因此，后续若再出现“机器人在线但不回复”，根因应继续排查：

- 平台消息事件是否投递
- 收到消息后是否进入正确路由
- 状态机与自由聊天是否发生冲突

## 相关文件

- `src/main.py`
- `src/runtime/single_instance.py`
- `run_logs/course_assistant.instance.json`
- `run_logs/course_assistant.instance.log`
- `run_logs/assistant.stderr.log`
